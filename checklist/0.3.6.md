# Checklist v0.3.6

目标：验证第 1~3 轮巡检改动在一致性、鲁棒性和边界场景下可回归、可发布。

## 1. 自动化回归

- [x] 运行 `npm run test`，确认全部通过（46 tests）。
- [x] 运行 `npm run compile`，确认无 TypeScript 编译错误。

## 2. 第 1 轮（一致性治理）

- [x] `package.nls*.json` 的支持格式描述已与当前能力一致（含 XML / CSV / TSV）。
- [x] README 多语言中的 LaTeX 命名与展示风格已统一。
- [x] `WIKI.md` 架构描述已与当前能力对齐（含 XML 与 CSV/TSV Provider）。

## 3. 第 2 轮（深度鲁棒性）

- [x] Webview message 入口增加类型归一化与防御校验（zoom、line range、baseUri、content 等）。
- [x] Markdown task checkbox 行号映射忽略 fenced code block 内伪 task。
- [x] Mermaid 预检支持前置注释后再声明图类型。
- [x] 以上改动已补充回归测试并通过。

## 4. 第 3 轮（边界场景）

- [x] Markdown 标题提取忽略 fenced code block 内伪标题，避免 locate 映射误判。
- [x] 新增对应回归测试并通过。

## 5. 版本与发布文件

- [x] `package.json` 与 `package-lock.json` 版本均为 `0.3.6`。
- [x] `README.md` / `README.zh-CN.md` / `README.zh-TW.md` / `README.zh-HK.md` / `README.ja-JP.md` 版本徽章已更新为 `0.3.6`。
- [x] `CHANGELOG.md` 已新增 `0.3.6` 发布记录。
- [x] 本次改动对应 checklist 已落地为 `checklist/0.3.6.md`。
- [x] 运行 `npx @vscode/vsce package` 并确认 VSIX 产物可安装（发布前手动步骤）。

## 6. 第 4 轮（Task D）

- [ ] JS / TS 注释样式按范围收敛：类与类成员函数使用多行 `/** */`，变量内部函数与其他注释使用 `//`。
- [ ] 注释处理遵循“已有注释尽量保留原文案含义，仅做必要格式转换”。
- [ ] `AGENTS.md` 已补充注释处理逻辑（含保留既有语义的约束）。
- [ ] `test/supported-files/csv.csv` 与 `test/supported-files/tsv.tsv` 已扩展为 30+ 行并覆盖特殊单元格场景。
- [ ] CSV / TSV 预览已支持首行（表头）与首列固定。
- [ ] README 多语言与 `CHANGELOG.md` 已同步更新以上能力。

## 7. 第 5 轮（Task E）

- [ ] CSV / TSV 首行在滚动表格时固定在顶部。
- [ ] CSV / TSV 首行与首列背景色为不透明样式。
- [ ] Interface / 类 / 类成员函数注释已补齐 `@param` / `@returns` / `@throws`。
- [ ] 注释描述与标签文案句末标点已清理，并同步更新 `AGENTS.md` 规则。

## 8. 第 6 轮（Task F）

- [ ] Interface / 类 / 类成员函数注释已清理占位标签，`@param` / `@returns` / `@throws` 仅保留有业务语义内容。
- [ ] `resources/preview.js` 与 `scripts/prepare-vendor.mjs` 的全局函数已统一为多行 `/** */` 注释风格。
- [ ] 全局配置项、硬编码常量、枚举集合统一为 `UPPER_SNAKE_CASE`，并补充用途注释。
- [ ] `AGENTS.md` 已同步 Task F 注释与命名规范。
